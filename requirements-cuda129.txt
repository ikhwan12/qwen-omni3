# PyTorch with CUDA 12.9 support (for latest GPUs)
--index-url https://download.pytorch.org/whl/cu129
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Install PyTorch first, then install other dependencies:
# pip install -r requirements-cuda129.txt
# pip install -r requirements.txt

