# PyTorch with CUDA 11.8 support (for older GPUs)
--index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Install PyTorch first, then install other dependencies:
# pip install -r requirements-cuda118.txt
# pip install -r requirements.txt

